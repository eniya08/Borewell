import ProjectsSection from "@/components/ProjectsSection";

const Projects = () => {
    return (
        <main className="pt-24">
            <ProjectsSection />
        </main>
    );
};

export default Projects;
