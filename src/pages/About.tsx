import AboutSection from "@/components/AboutSection";

const About = () => {
    return (
        <main className="pt-24">
            <AboutSection />
        </main>
    );
};

export default About;
