import GallerySection from "@/components/GallerySection";

const Gallery = () => {
    return (
        <main className="pt-24">
            <GallerySection />
        </main>
    );
};

export default Gallery;
